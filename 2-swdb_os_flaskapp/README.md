# OpenSource Products present in SWDD that are open source, and their categories.

```
data/libraries_with_links.csv comes from the scan made by the script in 9. inception folderand contains github links of libraries of libraries.
```